# multiple-server-client-chat
A threaded server(with mutex locks) which listens to multiple clients and can reply to them in a 'conversation'. Messages sent are encoded and  TCP/IP is to manage traffic.
